function Fillbtn({ text }: { text: string }) {
  return <button style={{ padding: "12px 24px", background: "#2aa86b", borderRadius: "6px", color: "#fff", fontWeight: "500" }}>{text}</button>;
}

export default Fillbtn;
